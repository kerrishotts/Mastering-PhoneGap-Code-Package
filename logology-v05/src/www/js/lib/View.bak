"use strict";

import Emitter from "yasmf-emitter";

export default class View extends Emitter {
    constructor() {
        super();
    }
    render({model, controller} = {}) {
        return undefined;
    }
    destroy() {
        super.destroy();
    }
}
